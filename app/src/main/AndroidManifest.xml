<manifest package="com.github.sonerik.bugtracktor"
          xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".App"
        >

        <activity
            android:name=".screens.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".screens.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:launchMode="singleTask"
            />

        <activity
            android:name=".screens.CreateProjectActivity"
            android:label="New Project"
            android:theme="@style/Theme.AppCompat.Light.Dialog"
            />

        <activity
            android:name=".screens.SelectProjectMemberActivity"
            android:label="Select Member"
            android:theme="@style/Theme.AppCompat.Light.Dialog"
            />

        <activity
            android:name=".screens.SelectIssueTypeActivity"
            android:label="Select Issue Type"
            android:theme="@style/Theme.AppCompat.Light.Dialog"
            />

        <activity
            android:name=".screens.AddMemberActivity"
            android:label="Add Member"
            android:theme="@style/AppDialog.NoTitle"
            />

        <activity
            android:name=".screens.ProjectActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".screens.IssueActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|keyboardHidden|screenSize"
            />

        <activity
            android:name=".screens.ProjectIssuesActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".screens.ProjectMembersActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />

    </application>

</manifest>
